[project]
name = "sample-bedrock-invocation-analytics"
version = "3.3.4"
description = "Bedrock invocation logging analytics - monitor token usage, costs and performance"
requires-python = ">=3.13"
dependencies = [
    "aws-cdk-lib>=2.253.0",
    "constructs>=10.0.0",
    "nicegui",
    "boto3",
    "pyyaml>=6.0.3",
]
